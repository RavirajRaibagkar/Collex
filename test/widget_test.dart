import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Collex app smoke test', (WidgetTester tester) async {
    // Tests will be added after Supabase is configured
    expect(true, isTrue);
  });
}
